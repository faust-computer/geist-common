{
  "name": "geist_common",
  "author": "Faust Computer",
  "version": "0.1.15",
  "main": "dist/msgs.js",
  "types": "dist/msgs.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.prod.json",
    "generate": "git submodule update --init --recursive && npx ros-typescript-generator",
    "release": "npm publish --access public",
    "test": "jest"
  },
  "files": [
    "/dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "description": "Geist common messages for running a Geist application"
}
